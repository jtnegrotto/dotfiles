# dotfiles

These are my current configuration files. Right now everything is fairly
Linux-specific, but soon I'll update them for macOS as well. The scripts
`install` and `update` are used to install the symlinks and update the
dependencies, respectively.
